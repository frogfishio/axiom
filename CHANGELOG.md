# Changelog

## 1.0.1 - 2026-03-19

This is the first corrected public SDA release on crates.io.

Published crates:

- `sda-lib` 1.0.1
- `sda` 1.0.1

Highlights:

- corrected crate `homepage` and `repository` metadata to point at `https://github.com/frogfishio/axiom`
- finalized public crates.io packaging for both the SDA library and CLI
- added crate-local READMEs and release documentation for the publish flow
- verified docs.rs builds for `sda-lib` 1.0.1 and `sda` 1.0.1

Registry cleanup:

- yanked `sda-lib` 1.0.0
- yanked `sda` 1.0.0

Notes:

- `1.0.0` was published successfully but contained placeholder repository metadata
- `1.0.1` is the intended install target for users